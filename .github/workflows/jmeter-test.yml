name: 🧪 Minimal Debug - Step 1

on:
  workflow_dispatch:

jobs:
  jmeter-test:
    runs-on: self-hosted
    steps:
      - name: ✅ Confirm runner prints output
        run: echo "🟢 Step 1 works — runner is alive and printing to GitHub UI"
